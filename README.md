# VectorlessRag

Frontend + backend monorepo for PageIndex vectorless RAG.

- Frontend: `frontend/` (Vite + React)
- Backend: `backend/` (FastAPI + PostgreSQL + Gemini)

Deployment steps: see `DEPLOYMENT.md`.
